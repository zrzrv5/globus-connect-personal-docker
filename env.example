# Globus Connect Personal Configuration
# Copy this file to .env and customize for your setup

# Endpoint Configuration
ENDPOINT_NAME=MyNAS-Endpoint
DESCRIPTION=Home NAS for data transfer
OWNER_EMAIL=user@example.com

# Data paths (adjust for your NAS)
DATA_DIR=./data
CONFIG_DIR=./config

# Additional mount paths (uncomment and adjust as needed)
# PHOTOS_DIR=/volume1/photos
# DOCUMENTS_DIR=/volume1/documents
# BACKUP_DIR=/mnt/backup

# Globus settings
RESTRICT_PATHS=rw/data
